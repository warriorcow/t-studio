---
import Container from './UI/Container.astro';
import Title from "./UI/Title.astro";
import Button from "./UI/Button.astro";
import {Image} from "astro:assets";
import InputEl from "./UI/InputEl.astro";
import CheckboxEl from "./UI/CheckboxEl.astro";
---

<Container gap>
  <div class="form">
    <div class="form__content">
      <Title color="secondary" tag="h2" text="Пообщаться с нами" hasArrow arrowPosition="right" />
      <div class="form__description">
        Оставьте заявку и мы свяжемся с вами в течении одного рабочего дня.
      </div>
    </div>
    <form class="form__inner">
      <InputEl type="text" placeholder="Как Вас зовут? *" />
      <InputEl type="text" placeholder="Название компании *" />
      <InputEl type="text" placeholder="Должность *" />
      <InputEl type="text" placeholder="Email *" />
      <InputEl type="text" placeholder="Номер телефона *" />
      <InputEl type="text" placeholder="Опишите кратко вашу задачу *" />

      <CheckboxEl>
        Я даю согласие на обработку персональных данных и согласен с <a href="#">условиями пользовательского соглашения и политики обработки персональных данных.</a>
      </CheckboxEl>

      <Button text="Отправить" />
    </form>
  </div>
</Container>

<style lang="scss" is:global>
  .form {
    position: relative;
    display: grid;
    grid-template-columns: 1fr 516px;
    border-radius: 20px;

    @include media(md) {
      grid-template-columns: 1fr;
      padding: 30px 0;

      &::before {
        position: absolute;
        top: 0;
        left: 0;
        content: "";
        width: calc(100% + 20px);
        height: 100%;
        margin: 0 -10px;
        background-image: url('../assets/form-bg.png');
        background-size: cover;
        background-repeat: no-repeat;
        background-position: 62%;
        z-index: -1;
      }
    }

    &__content {
      width: calc(100% + 40px);
      margin-right: -40px;
      padding: 40px 20px 40px 40px;
      color: $secondary;
      border-radius: 20px;
      background-image: url('../assets/form-bg.png');
      background-size: cover;
      background-repeat: no-repeat;

      @include media(md) {
        background: none;
        width: 100%;
        margin-right: 0;
        padding: 0;
      }
    }

    &__description {
      @include MontserratMedium26;

      margin-top: 20px;
      max-width: 395px;

      @include media(md) {
        @include MontserratMedium18;
      }
    }

    &__inner {
      display: flex;
      flex-direction: column;
      gap: 40px 0;
      padding: 50px;
      border-radius: 20px;
      background-color: $secondary;
      border: 1px solid $primary;

      @include media(md) {
        margin-top: 30px;
        padding: 30px 10px;
        gap: 30px 0;
      }
    }
  }
</style>

<script>

</script>